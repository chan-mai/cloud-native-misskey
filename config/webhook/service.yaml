apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: cloud-native-misskey-system
  labels:
    app.kubernetes.io/name: cloud-native-misskey
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-manager
