apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Validating/Mutating webhook入りバンドル。cert-manager必須
# webhook無しで動かす場合は config/default を使う
resources:
  - ../crd
  - ../rbac
  - ../manager
  - ../webhook
  - ../certmanager
patches:
  - path: manager_webhook_patch.yaml
